/**
 * Configuração do ambiente de produção.
 * Ajuste apiUrl conforme a URL do backend em produção.
 */
export const environment = {
  production: true,
  apiUrl: '/api', // Relativo quando frontend e backend estão no mesmo domínio
};
